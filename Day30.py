# Day30: Filtering and Sorting

# Filtering: In short, it means fetching the necessary data only at a specific time.
# The process of fetching the necessary data only at a specific time is known as filtering.
# df[df['Total_Marks'] > 80]

# Sorting: In short, it means arranging data in a particular format either in ascending or descending or both order. 
# Arranging data in a particular format either in ascending or descending or both order.
# df.sort_values(by='Total_Marks', ascending=False, inplace=True)

# sort_index: 
# It means sorting the DataFrame by its index.
# df.sort_index(inplace=True)

# reset_index
# It means resetting the index of the DataFrame.
# df.reset_index(drop=True,inplace=True)
